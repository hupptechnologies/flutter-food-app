import 'package:flutter/material.dart';

class PlaceReviewView extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text("Review VUEW"),
    );
  }
}