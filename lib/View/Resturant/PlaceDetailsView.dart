import 'package:flutter/material.dart';

class PlaceDetailView extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text("Place Detail View"),
    );
  }
}